```rust
# Dependencies
target/
Cargo.lock

# Logs and temp files
*.log
*.tmp

# Environment
.env
.env.local
*.env.*

# Editors
.vscode/
.idea/

# OS generated files
.DS_Store
Thumbs.db

# Coverage reports
coverage/
htmlcov/
.coverage

# Rust-specific
**/*.rs.bk
```

Wait, I need to reconsider this. Looking at the changes again - only source files were added/modified (`.rs` and `.tsx` files), and there's no indication of build artifacts or dependency directories in the change list that would require adding those patterns. Let me correct this:

(nothing)